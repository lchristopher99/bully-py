class UserAccount(self, firstName, lastName, user, pw, cardName, cardNum, cardExp, cardCVV, addr, city, state, zip):
    def __init__(self, firstName, lastName, user, pw, cardName, cardNum, cardExp, cardCVV, addr, city, state, zip):
        self.firstName = firstName,
        self.lastName = lastName,
        self.user = user,
        self.pw = pw,
        self.cardName = cardName,
        self.cardNum = cardNum,
        self.cardExp = cardExp,
        self.cardCVV = cardCVV,
        self.addr = addr,
        self.city = city,
        self.state = state,
        self.zip = zip
